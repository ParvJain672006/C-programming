//f(n) with array as argument calculate sum of array elements
#include<stdio.h>
int sumarray(int arr[], int size ){
	sum = sum + arr [i];
	return sum ; 
}
int main (){
	int numbers [ ]= {1,2,3,4,5,};
	int size = sizeof (numbers)/ size of (numbers[10]);
	printf("sum of array :%d\n", sumarray(numbers, size ));
	return 0 ; 
}
